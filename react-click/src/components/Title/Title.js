import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>Animal Click Game!</h1>
        <p>Click on an image to earn points.
            Don't click on any more than once!</p>
    </div>
);
export default Title;
